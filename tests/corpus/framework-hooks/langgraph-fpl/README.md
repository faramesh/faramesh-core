# Corpus: LangGraph single-agent (FPL policy path)

Same stack as `langgraph-single-agent`, but `tests/langgraph_single_agent_real_stack_fpl.sh` points governance at the FPL policy file (`FARAMESH_LANGGRAPH_REAL_POLICY`).
