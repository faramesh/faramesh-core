# Corpus: LangChain single-agent (FPL policy path)

Same real stack as `langchain-simple`, but `tests/langchain_single_agent_real_stack_fpl.sh` forces the FPL policy path via `FARAMESH_LANGCHAIN_REAL_POLICY` (see that script).

Use this row to track FPL-authored policy coverage alongside YAML-first LangChain harnesses.
