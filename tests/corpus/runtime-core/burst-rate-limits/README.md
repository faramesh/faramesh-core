# Burst rate limits (adapter harness)

Wraps `tests/burst_rate_limit_harness.sh`: burst and isolation behavior for `internal/adapter/sdk` and `internal/adapter/proxy`.

**WIP** in the corpus contract: governance plumbing tests, not an end-to-end agent matrix row.
