agent test-bonus {
  default deny

  rules {
    deny! shell/* when args.cmd != nil && args.cmd matches "rm -rf|shutdown|reboot|mkfs|format"
      reason: "dangerous shell commands blocked"
    defer shell/* reason: "shell commands require approval"
    defer kubernetes/* reason: "kubernetes operations require approval"
    permit http/get
  }
}
