agent deny-destructive-shell {
  default deny

  rules {
    deny! shell/* when args.cmd != nil && args.cmd matches "rm -rf|shutdown|reboot|mkfs|:(){:|:&};:"
      reason: "destructive shell commands blocked"
  }
}
