rules:
  - match:
      tool: "http"
      op: "*"
    allow: true
    description: "Allow all HTTP operations"

  - match:
      tool: "*"
      op: "*"
    deny: true
    description: "Default deny (first-match wins)"
